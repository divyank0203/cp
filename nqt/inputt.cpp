#include<bits/stdc++.h>
using namespace std;
int main(){
    // Array is given in string format without size:-
    // example -> "1 2 3 4 5"
    // string line;
    // getline(cin, line);
    // stringstream ss(line);
    // vector<int> arr;
    // int num;
    // while(ss>>num){
    //     arr.push_back(num);
    // }

    // Array given in string format separated by commas:-
    // example => "1,2,3,4,5"
    // string line;
    // getline(cin, line);
    // stringstream ss(line);
    // vector<int> arr;
    // string token;
    // while(getline(ss, token, ',')){
    //     arr.push_back(stoi(token));
    // }

    // Array given in strin format separated by commas and within square brackets:-
    // example => "[1,2,3,4,5]"
    // string line;
    // getline(cin, line);
    // line.erase(remove(line.begin(), line.end(), '['), line.end());
    // line.erase(remove(line.begin(), line.end(), ']'), line.end());
    // stringstream ss(line);
    // vector<int> arr;
    // string token;
    // while(getline(ss, token, ',')){
    //     arr.push_back(stoi(token));
    // }

    // To output float up to n decimal places:-
    // double num;
    // cin>>num;
    // cout<<fixed<<setprecision(n)<<num;
    // return 0;

    
    // 2d Array input given in string format comma separated:-
    // example => "1,2,3,4,5,6"; rows=3; cols=2;
    // int rows, cols;
    // cin>>rows>>cols;
    // string line;
    // getline(cin, line);
    // //remove spaces if it's "1, 2, 3, 4, 5, 6";
    // line.erase(remove(line.begin(), line.end(), ' '), line.end());
    // stringstream ss(line);
    // vector<int> nums;
    // string token;
    // while(getline(ss, token, ',')){
    //     nums.push_back(stoi(token));
    // }
    // int k=0;
    // vector<vector<int>> arr(rows, vector<int>(cols));
    // for(int i=0; i<rows; i++){
    //     for(int j=0; j<cols; j++){
    //         arr[i][j]=nums[k++];
    //     }
    // }

    
}