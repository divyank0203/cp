#include<bits/stdc++.h>
using namespace std;
using ll = long long;
using dll = double long;
int main(){
    ll t;
    cin>>t;
    while(t--){
        dll n;
        cin>>n;
        
        // WRONG APPROACH
        // pair<dll, dll> s = {6, 15};
        // pair<dll, dll> m = {8, 20};
        // pair<dll, dll> l = {10, 25};
        // pair<dll, dll> sm = {14, 35};
        // pair<dll, dll> sl = {16, 40};
        // pair<dll, dll> ml = {18, 45};
        // pair<dll, dll> sml = {24, 60};
        // ll s1 = ceil(n/s.first)*s.second;
        // ll m1 = ceil(n/m.first)*m.second;
        // ll l1 = ceil(n/l.first)*l.second;
        // ll sm1 = ceil(n/sm.first)*sm.second;
        // ll sl1 = ceil(n/sl.first)*sl.second;
        // ll ml1 = ceil(n/ml.first)*ml.second;
        // ll sml1 = ceil(n/sml.first)*sml.second;
        // cout<<min({s1, m1, l1, sm1, sl1, ml1, sml1})<<endl;

    }
}